name: SAST – CodeQL Analysis

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  schedule:
    - cron: "0 3 * * 1"

jobs:
  codeql-analysis:
    name: CodeQL SAST Scan
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Initialise CodeQL ───────────────────────────────────────
      - name: Initialise CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript        # valid identifier; covers TS too
          queries: security-extended   # broader ruleset than default

      # ── 3. Create SARIF output directory ───────────────────────────
      - name: Create SARIF output directory
        run: mkdir -p sarif-results

      # ── 4. Run the analysis ────────────────────────────────────────
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:javascript"
          output: sarif-results        # directory must already exist
          upload: true

      # ── 5. Block pipeline on any findings ──────────────────────────
      - name: Block pipeline on findings
        run: |
          SARIF_FILE=$(ls sarif-results/*.sarif 2>/dev/null | head -n 1)
          if [ -z "$SARIF_FILE" ]; then
            echo "No SARIF file found – treating as clean."
            exit 0
          fi
          RESULT_COUNT=$(jq '[.runs[].results // [] | length] | add // 0' "$SARIF_FILE")
          echo "CodeQL findings: $RESULT_COUNT"
          if [ "$RESULT_COUNT" -gt 0 ]; then
            echo "::error::SAST gate failed – $RESULT_COUNT vulnerabilit(y/ies) detected. Review the Security tab."
            exit 1
          fi
          echo "No vulnerabilities found. Pipeline continues."