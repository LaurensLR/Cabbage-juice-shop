name: SAST – CodeQL Analysis

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  schedule:
    - cron: "0 3 * * 1"

jobs:
  codeql-analysis:
    name: CodeQL SAST Scan
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      actions: read
      contents: read
      security-events: write
      packages: read

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 1b. Detect supported source ──────────────────────────────
      - name: Detect JavaScript/TypeScript source
        id: detect_source
        run: |
          if git ls-files '*.js' '*.jsx' '*.ts' '*.tsx' '*.mjs' '*.cjs' | grep -q .; then
            echo "has_js_ts=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_js_ts=false" >> "$GITHUB_OUTPUT"
            echo "No JavaScript/TypeScript source detected; skipping CodeQL scan."
          fi

      # ── 2. Initialise CodeQL ───────────────────────────────────────
      - name: Initialise CodeQL
        if: steps.detect_source.outputs.has_js_ts == 'true'
        uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript   # correct v3 extractor name
          build-mode: none                   # skip build tracing; scan files directly
          queries: security-extended

      # ── 3. Run the analysis ────────────────────────────────────────
      - name: Perform CodeQL Analysis
        if: steps.detect_source.outputs.has_js_ts == 'true'
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:javascript-typescript"
          output: sarif-results
          upload: true

      # ── 4. Block pipeline on any findings ──────────────────────────
      - name: Block pipeline on findings
        if: steps.detect_source.outputs.has_js_ts == 'true'
        run: |
          SARIF_FILE=$(ls sarif-results/*.sarif 2>/dev/null | head -n 1)
          if [ -z "$SARIF_FILE" ]; then
            echo "No SARIF file found – treating as clean."
            exit 0
          fi
          RESULT_COUNT=$(jq '[.runs[].results // [] | length] | add // 0' "$SARIF_FILE")
          echo "CodeQL findings: $RESULT_COUNT"
          if [ "$RESULT_COUNT" -gt 0 ]; then
            echo "::error::SAST gate failed – $RESULT_COUNT vulnerabilit(y/ies) detected. Review the Security tab."
            exit 1
          fi
          echo "No vulnerabilities found. Pipeline continues."

      - name: Skip SAST when no supported source is present
        if: steps.detect_source.outputs.has_js_ts != 'true'
        run: echo "CodeQL scan skipped because this repository does not contain JavaScript/TypeScript source files."